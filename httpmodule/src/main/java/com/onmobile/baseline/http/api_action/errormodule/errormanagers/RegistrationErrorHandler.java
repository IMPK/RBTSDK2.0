package com.onmobile.baseline.http.api_action.errormodule.errormanagers;

/**
 * Created by Nikita Gurwani .
 */
public class RegistrationErrorHandler extends APIErrorHandler {

    /*public static String handleRegistrationErr(ErrorDTO errorResponse) {
        String s = "";
        ErrorsCodes.RegistrationErrorCodes codes = (ErrorsCodes.RegistrationErrorCodes) errorResponse.getCode();
        if (errorResponse.getCode() != null) {
            switch (codes) {
                case INVALID_PIN:
                    //s = ErrorString.error_handler_mobile_network;
                    break;
                case INVALID_PARAMETER:
                    //s = ErrorString.error_handler_mobile_network;
                    break;
                default:
                    s = handleGenericError(errorResponse);
                    break;
            }
        }
        return s;
    }*/
}
